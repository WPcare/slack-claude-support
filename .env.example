# Slack Bot Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack Signing Secret (from App Credentials)
SLACK_SIGNING_SECRET=your-signing-secret

# Slack App Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token

# Optional: Path to Claude CLI (defaults to ~/.local/bin/claude)
# CLAUDE_PATH=/path/to/claude

# Optional: Timeout in milliseconds (defaults to 120000 = 2 minutes)
# CLAUDE_TIMEOUT=120000
